# Issue Tracker

Default tracker: local files.

Store generated issues in `docs/issues/` as Markdown. If the project later moves to GitHub Issues or Linear, keep the same issue shape and add external links at the top of each file.
