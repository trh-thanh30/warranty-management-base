/**
 * Membership data and endpoints are retained, but dealer scoping is temporarily
 * disabled so authorised back-office users can access the full dealer directory.
 */
export const DEALER_MEMBERSHIP_ACCESS_ENABLED = false;
