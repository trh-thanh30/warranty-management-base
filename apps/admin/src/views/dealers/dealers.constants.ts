export const DEALERS_PAGE_SIZE = 10;

// Keep membership implementation available while temporarily hiding the UI.
export const DEALER_MEMBERSHIP_UI_ENABLED = false;

export const DEALER_STATUS_FILTERS = ["ALL", "ACTIVE", "INACTIVE"] as const;
